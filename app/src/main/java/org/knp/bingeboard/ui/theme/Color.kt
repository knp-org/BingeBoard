package org.knp.bingeboard.ui.theme

import androidx.compose.ui.graphics.Color

// ── Liquid Glass: Monochromatic Purity ──────────────────────────
// The entire UI relies on grayscale, deep blacks, pure whites,
// and varying levels of opacity. No bright brand colors.

// ── Dark Mode Colors ────────────────────────────────────────────
val DarkBackground = Color(0xFF050505)
val DarkSurface = Color(0xFF0A0A0A)
val DarkSurfaceVariant = Color(0xFF141414)

val DarkOnBackground = Color(0xFFFFFFFF)
val DarkOnSurface = Color(0xFFFFFFFF)
val DarkOnSurfaceVariant = Color(0xFFA0A0A0)

// ── Light Mode Colors ───────────────────────────────────────────
val LightBackground = Color(0xFFE2E8F0)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFEEEEEE)

val LightOnBackground = Color(0xFF0A0A0A)
val LightOnSurface = Color(0xFF141414)
val LightOnSurfaceVariant = Color(0xFF808080)

// ── Glass Colors ────────────────────────────────────────────────
val GlassDarkBg = Color(0x0DFFFFFF)        // 5% white
val GlassDarkBorder = Color(0x1AFFFFFF)    // 10% white
val GlassLightBg = Color(0x99FFFFFF)       // 60% white
val GlassLightBorder = Color(0x1A000000)   // 10% black

// ── Accent: Pure White for Active/Emphasis ──────────────────────
val AccentWhite = Color(0xFFFFFFFF)
val AccentGray = Color(0xFFB3B3B3)

// ── Functional Status Colors (muted, monochrome-adjacent) ───────
val StatusActive = Color(0xFF22C55E)      // Running/Airing (kept green for status)
